import { Button } from '@/components/ui/button';

export function ContactForm() {
    return (
        <div>
            <Button variant="default" className="mx-auto w-full max-w-xl">
                Send Message
            </Button>
        </div>
    );
}

